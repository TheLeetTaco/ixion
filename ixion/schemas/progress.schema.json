{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "ixion/schemas/progress.schema.json",
  "title": "Work progress (progress.json)",
  "description": "Runtime state for work. Single completion list per mode. Atomic writes via progress.json.tmp -> mv.",
  "type": "object",
  "additionalProperties": false,
  "required": ["schema_version", "mode", "status", "completed", "in_progress", "artifacts"],
  "properties": {
    "schema_version": { "const": 1 },
    "mode": { "enum": ["plan", "fix-findings"] },
    "status": { "enum": ["pending", "in_progress", "completed"] },
    "completed": {
      "type": "array",
      "items": { "type": "string", "minLength": 1 },
      "description": "Item IDs completed. In plan mode: phase IDs. In fix-findings mode: finding IDs (or array indices as strings)."
    },
    "in_progress": { "type": ["string", "null"] },
    "artifacts": {
      "type": "object",
      "additionalProperties": false,
      "required": ["files_modified", "commands_run"],
      "properties": {
        "files_modified": { "type": "array", "items": { "type": "string" } },
        "commands_run": {
          "type": "array",
          "description": "Forensic log of commands run during the chunk. Each entry may be a free-form summary string OR a structured object with command/exit_code/stdout_tail. Nothing downstream reads this programmatically; the structure is advisory.",
          "items": {
            "oneOf": [
              { "type": "string", "minLength": 1 },
              {
                "type": "object",
                "additionalProperties": false,
                "required": ["command", "exit_code"],
                "properties": {
                  "command": { "type": "string", "minLength": 1 },
                  "exit_code": { "type": "integer" },
                  "stdout_tail": { "type": "string" }
                }
              }
            ]
          }
        },
        "checkpoint_commits": {
          "type": "array",
          "description": "One entry per chunk committed at its checkpoint, written immediately after that chunk's commit. Names the same chunks as completed[] — a failed commit removes the chunk from completed[] and writes no entry here, leaving it in neither list. A chunk in completed[] with no entry here means the process died between those two writes; resume looks the commit up by its Ixion-Chunk trailer and either re-appends the entry or, if the commit never landed, drops the chunk from completed[] so its wave re-runs it.",
          "items": {
            "type": "object",
            "additionalProperties": false,
            "required": ["chunk_id", "sha"],
            "properties": {
              "chunk_id": { "type": "string", "minLength": 1 },
              "sha": { "type": "string", "pattern": "^[0-9a-f]{7,40}$" }
            }
          }
        }
      }
    },
    "error_log": { "type": "array", "items": { "type": "string" } }
  }
}
